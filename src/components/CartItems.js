import React, { Component } from 'react';
import View from '../views/CartItems'

class cartItems extends Component {
    render() {
        return (
            <View/>
        );
    }
}

export default cartItems;
